<section class="py-20 font-infant flex items-center justify-center text-center gap-4 lg:gap-14">
    <article>
        <div id="days" class="text-4xl lg:text-8xl font-alexBrush">00</div>
        <div class="lg:text-2xl">Días</div>
    </article>

    <article class="text-2xl lg:text-4xl">:</article>

    <article>
        <div id="hours" class="text-4xl lg:text-8xl font-alexBrush">00</div>
        <div class="lg:text-2xl">Horas</div>
    </article>

    <article class="text-2xl lg:text-4xl">:</article>

    <article>
        <div id="minutes" class="text-4xl lg:text-8xl font-alexBrush">00</div>
        <div class="lg:text-2xl">Minutos</div>
    </article>

    <article class="text-2xl lg:text-4xl">:</article>

    <article>
        <div id="seconds" class="text-4xl lg:text-8xl font-alexBrush">00</div>
        <div class="lg:text-2xl">Segundos</div>
    </article>
</section>

<script>
const targetDate = new Date("2026-06-20T19:30:00").getTime();

function updateCountdown() {
    const now = new Date().getTime();
    const diff = targetDate - now;

    if (diff <= 0) return;

    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
    const minutes = Math.floor((diff / (1000 * 60)) % 60);
    const seconds = Math.floor((diff / 1000) % 60);

    document.getElementById("days").textContent = String(days).padStart(2, "0");
    document.getElementById("hours").textContent = String(hours).padStart(2, "0");
    document.getElementById("minutes").textContent = String(minutes).padStart(2, "0");
    document.getElementById("seconds").textContent = String(seconds).padStart(2, "0");
}

updateCountdown();
setInterval(updateCountdown, 1000);
</script>
